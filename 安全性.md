# 安全性修改

> 之前的代码有一些安全性问题，在这里做出改正：

1. 重复用户注册：原来一个主机上可以多次重复注册用户，因为只使用用户名+密码来生成密钥，所以对于一些不安全的密码还是有可能发生公钥的碰撞的，还有可能会向网络中发送垃圾信息以及进行攻击，这时候就会对数据完整性有所挑战。这里通过限制用户注册数量、限制用户注册频率、工作量证明等机制，减小用户注册的频率
> 对应修改：host_register、pow、login
2. 重放攻击：原来的代码中没有针对重放攻击做出修改。这意味着攻击者接获一条信息之后可以重复发送给对方，对方也能正确接收。现在通过加入时间窗口（读取时间戳，只接受五分钟内的消息）、消息指纹（将接收到的消息放入内存缓冲区中，丢弃相同信息）来避免重放。
> 对应修改：replayProjection
3. 群组消息安全性：虽然只有有邀请码（群组私钥）的人才能解密，但是任然有以下问题：
   1. 静态密钥：项目中的消息是有缓存的，如果攻击者获取密钥后，可能会破解之前的重要消息；同时如果一个用户不在群众后，仍可以解密。
   2. 缺乏不可否认性：群组内的成员可以伪造对方身份发送信息，同时攻击者也可以对密文加以修改。

    现在做出修改：当用户成员发生改变时，需要更新密钥，同时使用原来的密钥加密后点对点发送给群组中的其他人（当用户在节点上时，立即发送）；加入数字签名。