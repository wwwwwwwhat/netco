# 前端使用说明

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动服务器

```bash
npm run server
```

服务器将在 `http://localhost:3000` 启动。

### 3. 打开浏览器

访问 `http://localhost:3000` 即可使用前端界面。

## 功能说明

### 登录/注册

- 输入用户名和密码（系统会根据用户名和密码生成密钥对）
- 端口号可选，留空会自动分配
- 点击"登录 / 注册"按钮

### 群组功能

#### 创建群组
1. 点击侧边栏的"群组"标签
2. 点击"+ 创建"按钮
3. 输入群组名称
4. 创建成功后，会显示群组ID和共享密钥
5. **重要**: 保存群组ID和共享密钥，以便其他人加入

#### 加入群组
1. 在"加入群组"区域输入：
   - 群组ID
   - 群组名称
   - 共享密钥（创建者提供的密钥）
2. 点击"加入"按钮

#### 发送群组消息
1. 在侧边栏点击要聊天的群组
2. 在主界面输入消息
3. 点击"发送"或按Enter键

### 私聊功能

#### 开始新对话
1. 点击侧边栏的"私聊"标签
2. 点击"+ 新对话"按钮
3. 输入：
   - 对方的公钥（可以从网络标签页查看自己的公钥）
   - 对方的用户名
4. 点击"开始对话"

#### 发送私聊消息
1. 在侧边栏点击要聊天的对话
2. 在主界面输入消息
3. 点击"发送"或按Enter键

### 网络信息

点击"网络"标签可以查看：
- 节点ID
- 连接的对等节点数量
- 监听地址

## 技术架构

### 前端
- **HTML/CSS/JavaScript**: 原生Web技术，无需构建工具
- **WebSocket**: 实时消息推送
- **RESTful API**: 与后端通信

### 后端API
- **Express.js**: HTTP服务器
- **WebSocket**: 实时消息推送
- **libp2p**: P2P网络层

### 安全特性
- ✅ 端到端加密（群组使用对称加密，私聊使用非对称加密）
- ✅ 密钥从用户名和密码派生（无密码存储）
- ✅ 消息通过P2P网络传播（元数据隐私）

## 文件结构

```
public/
├── index.html      # 主HTML文件
├── styles.css      # 样式文件
└── app.js          # 前端逻辑

src/
├── server.js       # API服务器
└── ...            # 后端代码
```

## 注意事项

1. **公钥交换**: 要开始私聊，需要先获取对方的公钥。可以通过网络标签页查看自己的公钥。

2. **群组密钥**: 创建群组后，必须保存群组ID和共享密钥。丢失密钥将无法解密群组消息。

3. **多设备**: 同一用户名和密码在不同设备上会生成相同的密钥对，因此可以在不同设备上使用。

4. **网络连接**: 确保防火墙允许P2P连接，或者使用相同的网络环境。

## 故障排除

### WebSocket连接失败
- 检查服务器是否正在运行
- 检查浏览器控制台是否有错误信息
- 尝试刷新页面

### 无法接收消息
- 检查是否已加入群组或开始对话
- 确认对方也在线并已连接
- 查看浏览器控制台的错误信息

### 消息无法解密
- 确认群组密钥是否正确
- 确认对方公钥是否正确
- 检查网络连接是否正常

