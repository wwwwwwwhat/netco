# 快速启动指南

## 安装依赖

```bash
npm install
```

## 启动服务器

```bash
npm run server
```

服务器将在 `http://localhost:3000` 启动。

## 使用步骤

### 1. 打开浏览器

访问 `http://localhost:3000`

### 2. 登录/注册

- 输入用户名和密码
- 点击"登录 / 注册"
- 系统会自动生成密钥对（基于用户名和密码）

### 3. 测试群组聊天

#### 用户A（创建群组）：
1. 点击"群组"标签
2. 点击"+ 创建"按钮
3. 输入群组名称（如"测试群组"）
4. **保存显示的群组ID和共享密钥**

#### 用户B（加入群组）：
1. 在另一个浏览器窗口或设备上登录（不同用户名）
2. 点击"群组"标签
3. 在"加入群组"区域输入：
   - 群组ID（从用户A获取）
   - 群组名称
   - 共享密钥（从用户A获取）
4. 点击"加入"

#### 发送消息：
1. 在侧边栏点击群组
2. 输入消息并发送
3. 双方都能看到消息

### 4. 测试私聊

#### 获取公钥：
1. 点击"网络"标签
2. 查看"我的公钥"
3. 点击公钥可复制到剪贴板

#### 开始对话：
1. 用户A：点击"私聊"标签 → "+ 新对话"
2. 输入用户B的公钥和用户名
3. 用户B：同样操作，输入用户A的公钥和用户名
4. 双方开始对话并发送消息

## 多设备测试

可以在不同设备上使用相同的用户名和密码登录，系统会生成相同的密钥对。

## 注意事项

- 群组密钥必须保存，丢失后无法解密历史消息
- 私聊需要对方的公钥
- 确保设备在同一网络或P2P网络可连接

